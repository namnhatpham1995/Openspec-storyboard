# Go build artifacts
/storyboard
/storyboard.exe
/dist/
*.test
*.out

# Frontend dependencies. The production bundle is committed so Go's embed
# directive can build from a clean checkout without requiring Node.js.
frontend/node_modules/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# AI planning/tooling artifacts (OpenSpec change proposals, Claude Code
# and Codex command/skill config) - local working state, not committed.
# Root-anchored so this never matches internal/openspec/ (real Go source).
/openspec/
/.claude/
/.codex/
/AGENTS.md
