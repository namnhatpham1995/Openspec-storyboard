// Package registry persists the list of project folders the user has
// registered with Storyboard, along with recent-projects state, to a
// single JSON file in the OS user-config directory (design.md decision D7).
package registry
