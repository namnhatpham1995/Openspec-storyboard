// Package server exposes the JSON REST API and SSE event stream, serves
// the embedded frontend build with SPA fallback routing, and wires the
// openspec, registry, and watch packages together over HTTP.
package server
